# Configuration used by bootstrap.sh

git:
  gitconfig: true

oh-my-zsh:
  zshrc: true
  # Note that the zshrc symlink also automatically covers pointing to the zsh-custom directory.

vscode:
  settings: true
  extensions: true

cursor:
  settings: true
  extensions: true

iterm2:
  preferences: true

claude:
  settings: true
