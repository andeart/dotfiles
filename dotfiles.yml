---
# Configuration used by bootstrap.sh

# The zshrc symlink also covers pointing to the zsh-custom directory.
# oh-my-zsh:
#   zshrc: true

# git:
#   gitconfig: true

# markdownlint:
#   config: true

# vscode:
#   settings: true
#   extensions: true

# Links the whole ~/.warp directory, not individual files, because Warp writes
# into it at runtime (settings.toml on every toggle, themes on install).
# warp:
#   config: true

# agents-and-claude:
#   sync: true
