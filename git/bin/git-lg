#!/usr/bin/env bash
args=()
[ -n "$1" ] && args+=("-$1")
git log --color --graph \
  --pretty=format:'%C(bold yellow)%h%Creset%C(auto)%d%Creset %C(bold blue)%cd%Creset %s %C(bold magenta)-%an%Creset' \
  --date=format-local:'%Y-%m-%d %H:%M' \
  --abbrev-commit "${args[@]}" | sed 's/\*/•/g'
